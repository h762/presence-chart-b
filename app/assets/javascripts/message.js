$(function(){
  $('.chartBtnA1').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      console.log(this)
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnA2').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnA3').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnA4').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });
  $('.chartBtnA5').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnA6').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnA7').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnA8').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });
  $('.chartBtnA9').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnA10').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnA11').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnA12').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });
  $('.chartBtnA13').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnA14').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnA15').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnA16').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });
  $('.chartBtnA17').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnA18').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnA19').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnA20').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });
  $('.chartBtnA21').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnA22').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnA23').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnA24').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnB1').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnB2').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnB3').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnB4').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });
  $('.chartBtnB5').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnB6').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnB7').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnB8').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });
  $('.chartBtnB9').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnB10').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnB11').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnB12').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnB13').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnB14').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnB15').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });
  $('.chartBtnB16').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnB17').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnB18').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnB19').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnB20').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnB21').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnB22').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnB23').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnB24').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnC1').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnC2').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnC3').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnC4').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });
  $('.chartBtnC5').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnC6').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnC7').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnC8').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });
  $('.chartBtnC9').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnC10').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnC11').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnC12').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnC13').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnC14').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnC15').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });
  $('.chartBtnC16').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnC17').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnC18').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnC19').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnC20').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnC21').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnC22').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnC23').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnC24').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnD1').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnD2').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnD3').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnD4').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });
  $('.chartBtnD5').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnD6').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnD7').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnD8').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });
  $('.chartBtnD9').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnD10').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnD11').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnD12').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnD13').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnD14').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnD15').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });
  $('.chartBtnD16').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnD17').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnD18').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnD19').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnD20').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnD21').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnD22').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnD23').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });

  $('.chartBtnD24').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });


  $('.chartBtnS1').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });
  $('.chartBtnS2').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });
  $('.chartBtnS3').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });
  $('.chartBtnS4').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });
  $('.chartBtnS5').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });
  $('.chartBtnS6').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });
  $('.chartBtnS7').click(function(e){
    e.preventDefault()
    var target = $(this).text();
    if (target.match(/□/)){
      var result = target.replace("□", "■");
      $(this).parent().css({"background-color": "red"});
    }
    else {
      var result = target.replace("■", "□");
      $(this).parent().css({"background-color": "white"});
    }
    $(this).text(result);
  });
});